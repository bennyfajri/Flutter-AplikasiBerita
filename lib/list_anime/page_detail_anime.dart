import 'package:flutter/material.dart';
import 'package:tugas_akhir_flutter/model/model_anime.dart';

class DetailListFilm extends StatelessWidget {
  final ModelAnime model;
  DetailListFilm(this.model);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          // title: model.,
          ),
    );
  }
}
